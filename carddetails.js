function openCardModal(id) {
    const cardModal = document.getElementById(id);
    cardModal.showModal();
}
function closeCardModal(id) {
    const cardModal = document.getElementById(id);
    cardModal.close();
}